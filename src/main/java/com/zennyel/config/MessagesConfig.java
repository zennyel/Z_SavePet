package com.zennyel.config;

import com.zennyel.SavePets;

public class MessagesConfig extends CustomConfiguration{
    public MessagesConfig(SavePets instance, String file) {
        super(instance, file);
    }
}
