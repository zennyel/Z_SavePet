package com.zennyel.config.pet;

import com.zennyel.SavePets;
import com.zennyel.config.CustomConfiguration;

public class PetBoxConfig extends CustomConfiguration {
    public PetBoxConfig(SavePets instance, String file) {
        super(instance, file);
    }
}
