package com.zennyel.config.pet;

import com.zennyel.SavePets;
import com.zennyel.config.CustomConfiguration;

public class PetFusionConfig extends CustomConfiguration {
    public PetFusionConfig(SavePets instance, String file) {
        super(instance, file);
    }
}
